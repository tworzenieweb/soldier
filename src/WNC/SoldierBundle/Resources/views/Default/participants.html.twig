<div class="row">
    <div class="three columns text-left"><small>NAME</small></div>
    <div class="five columns text-left"><small>CITY/SCHOOL</small></div>
    <div class="four columns text-left"><small>RIGHTEOUS DEEDS</small></div>
</div>
{% for participant in participants %}

   <div class="row">
    <div class="three columns text-left"><a href="{{ path('participant_show', {'id': participant.participant.id}) }}">{{ participant.firstname }}</a>
    </div>
    <div class="five columns text-left">{{ participant.participant.city.name }}
    </div>
    <div class="four columns text-left">{{ participant.participant.activity.name }}
    </div>
</div>     
{% else %}
Currently no participants in database, please be patient.
{% endfor %}

</div>